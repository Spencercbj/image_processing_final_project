# pip install -r requirements.txt
# Install torch/torchvision FIRST from the CUDA 12.4 index:
#   pip install --index-url https://download.pytorch.org/whl/cu124 torch==2.6.0 torchvision==0.21.0
# Then the rest:
diffusers==0.29.2
transformers==4.45.2
accelerate==0.28.0
pytorch-lightning==2.2.1
safetensors==0.7.0
omegaconf==2.3.0
timm==1.0.27
basicsr==1.4.2
facexlib==0.3.0
gfpgan==1.3.8
einops==0.7.0
opencv-python==4.13.0.92
scikit-image==0.25.2
scipy==1.15.3
numpy==2.2.6
pillow==12.2.0
# DO NOT install xformers — it replaces the CUDA torch build with a CPU-only one.
